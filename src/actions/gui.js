import * as types from '../constants/ActionTypes';

export const toggleLeftNav = () => ({ type: types.TOGGLE_LEFT_NAV });

// @todo: To remove when we will add another actions
export const toRemoveAction = () => ({ type: types.TO_REMOVE_ACTION });
